Module Program
    Sub Main(args As String())
        'Console.WriteLine("NumLock : ")

        ''ファイルのパスを変数に格納
        'Dim strFilePath As String = "C:\_\sam.cmd"

        ''File.Existsでファイルの存在チェック
        'Dim boolFile_Exists As Boolean
        'boolFile_Exists = System.IO.File.Exists(strFilePath)

        ''FileInfo + Existsでファイルの存在チェック
        'Dim boolFileInfo As Boolean
        'Dim finfo As New System.IO.FileInfo(strFilePath)
        'boolFileInfo = finfo.Exists()

        ''結果をログに出力
        'Console.WriteLine("File.Exists：" & boolFile_Exists)
        'Console.WriteLine("FileInfo + Exists：" & boolFileInfo)
    End Sub
End Module
